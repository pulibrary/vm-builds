#cloud-config
autoinstall:
  version: 1
  locale: en_US
  keyboard:
    layout: us
  ssh:
    install-server: true
    allow-pw: true
  late-commands:
    - |
      if [ -d /sys/firmware/efi ]; then
        apt-get install -y efibootmgr
        efibootmgr -o $(efibootmgr | perl -n -e '/Boot(.+)\* ubuntu/ && print $1')
      fi
  user-data:
    preserve_hostname: false
    hostname: lib-vm
    package_upgrade: true
    timezone: America/New_York
    users:
      - name: pulsys
        passwd: $6$xyz$/DjDsU5JT6atBHRzRf6RcSQ.usUFOYX9sKv2SD9tBIM331WY.mzWrCuPj9rAi7pu3iOfQAEZ363TqRsK4QfB3/
        groups: [adm, cdrom, dip, plugdev, lxd, sudo]
        lock-passwd: false
        sudo: ALL=(ALL) NOPASSWD:ALL
        shell: /bin/bash
